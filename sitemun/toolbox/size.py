def text_size(text):
    text *= 1
    text = str(text) + 'em'
    return text


def title_size(text):
    text *= 1.8
    text = str(text) + 'em'
    return text


def section_title_size(text):
    text *= 2.8
    text = str(text) + 'em'
    return text
